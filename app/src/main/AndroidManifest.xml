<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.class10.intimecashmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".IncomeExpenseList" />
        <activity android:name=".StatisticGraph" />
        <activity android:name=".EnvironmentSetting" />
        <activity android:name=".SubAtcivities.IncomeInsert" />
        <activity android:name=".SubAtcivities.ExpenseInsert" />
        <activity
            android:name=".SubAtcivities.IntroSplash"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SubAtcivities.CategoryManager"
            android:theme="@style/Theme.MyDialog"/>
    </application>

</manifest>